import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Profile',
  description: 'Set up the profile of your chess account.',
};

export default function Profile() {
  return <div>Profile</div>;
}
